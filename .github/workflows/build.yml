name: build
on:
  push:
    branches: [ develop ]
jobs:
  build-lin:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    - name: My second step
      uses: IT-Medved/setup-onec-build-env-action@develop
